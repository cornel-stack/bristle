// Canonical site origin — source of truth for absolute metadata URLs
// (og:url, og:image, metadataBase). No trailing slash; origin only.
export const SITE_URL = "https://bristle.vercel.app" as const;
